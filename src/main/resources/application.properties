# Application Settings
spring.application.name=BellaVitaClone
server.port=8080

# Database Configuration (PostgreSQL via Docker)
spring.datasource.url=jdbc:postgresql://localhost:5432/bellavitadb
spring.datasource.username=bellavita
spring.datasource.password=bellavita123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=BellaVitaCloneSecretKey2024ForJWTTokenGenerationAndValidation
jwt.expiration=86400000

# CORS
spring.mvc.cors.allowed-origins=http://localhost:5173,http://localhost:3000

